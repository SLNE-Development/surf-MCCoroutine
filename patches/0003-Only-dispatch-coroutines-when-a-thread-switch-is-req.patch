From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: twisti <76837088+twisti-dev@users.noreply.github.com>
Date: Sat, 16 May 2026 17:01:44 +0200
Subject: [PATCH] Only dispatch coroutines when a thread switch is required

All Folia dispatchers unconditionally returned true from
isDispatchNeeded(), so every coroutine resumption went through a
scheduler round-trip even when the current thread was already allowed
to execute the task. Now:

* RegionDispatcher and EntityDispatcher return false when the current
  thread already owns the target region or entity
* GlobalRegionDispatcher returns false when already on the global tick
  thread
* MinecraftCoroutineDispatcher returns false when already on the
  primary thread (and no longer runs tasks inline just because the
  plugin is disabled)
* MainDispatcher returns false when already on its dedicated main
  thread; its threadId field is now volatile so the check reads a
  consistent value from any thread

kotlinx.coroutines then executes the continuation inline instead of
scheduling it. The async dispatchers still always dispatch; their
kdocs now document that contract.

diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncCoroutineDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncCoroutineDispatcher.kt
index 2a0e90a2e1346781790d5af0fe2e56592bb8a133..e23e6da7bc5acde8132909532226d1b0f19f324c 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncCoroutineDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncCoroutineDispatcher.kt
@@ -12,12 +12,12 @@ internal open class AsyncCoroutineDispatcher(
     private val plugin: Plugin,
     private val wakeUpBlockService: WakeUpBlockServiceImpl
 ) : CoroutineDispatcher() {
+    // surf start - enhance kdocs
     /**
-     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
-     * The default behavior for most dispatchers is to return `true`.
-     * This method should generally be exception-safe. An exception thrown from this method
-     * may leave the coroutines that use this dispatcher in the inconsistent and hard to debug state.
+     * Always returns true: async tasks must always be dispatched to the async thread pool,
+     * regardless of the current thread.
      */
+    // surf end - enhance kdocs
     override fun isDispatchNeeded(context: CoroutineContext): Boolean {
         wakeUpBlockService.ensureWakeup()
         return true
diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncFoliaCoroutineDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncFoliaCoroutineDispatcher.kt
index 428e93d0c31dcbe139be3a9f54cf997b43bc03c8..c73016cebdeade3dc302bef5c38aabff13dc8c7f 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncFoliaCoroutineDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/AsyncFoliaCoroutineDispatcher.kt
@@ -12,12 +12,12 @@ internal open class AsyncFoliaCoroutineDispatcher(
     private val plugin: Plugin,
     private val wakeUpBlockService: WakeUpBlockServiceImpl
 ) : CoroutineDispatcher() {
+    // surf start - enhance kdocs
     /**
-     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
-     * The default behavior for most dispatchers is to return `true`.
-     * This method should generally be exception-safe. An exception thrown from this method
-     * may leave the coroutines that use this dispatcher in the inconsistent and hard to debug state.
+     * Always returns true: async tasks must always be dispatched to Folia's async scheduler,
+     * regardless of the current thread.
      */
+    // surf end - enhance kdocs
     override fun isDispatchNeeded(context: CoroutineContext): Boolean {
         wakeUpBlockService.ensureWakeup()
         return true
diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/EntityDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/EntityDispatcher.kt
index 825d7add3c9848dd3088c7e8015b2057daeadd6f..1837c538a0a5daf46be03227982de424abfa234f 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/EntityDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/EntityDispatcher.kt
@@ -11,15 +11,14 @@ internal open class EntityDispatcher(
     private val wakeUpBlockService: WakeUpBlockServiceImpl,
     private val entity: Entity
 ) : CoroutineDispatcher() {
+    // surf start - enhance kdocs
     /**
-     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
-     * The default behavior for most dispatchers is to return `true`.
-     * This method should generally be exception-safe. An exception thrown from this method
-     * may leave the coroutines that use this dispatcher in the inconsistent and hard to debug state.
+     * Returns false if the current thread already owns the entity's region, allowing inline execution.
      */
+    // surf end - enhance kdocs
     override fun isDispatchNeeded(context: CoroutineContext): Boolean {
         wakeUpBlockService.ensureWakeup()
-        return true
+        return !plugin.server.isOwnedByCurrentRegion(entity) // surf - only schedule tasks if needed
     }
 
     /**
diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/GlobalRegionDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/GlobalRegionDispatcher.kt
index 660471f4b710f8e9b25d4c1152b9811b039c4407..2009a74442b109da213edd5bcb4709b7a4f14390 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/GlobalRegionDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/GlobalRegionDispatcher.kt
@@ -9,15 +9,14 @@ internal open class GlobalRegionDispatcher(
     private val plugin: Plugin,
     private val wakeUpBlockService: WakeUpBlockServiceImpl
 ) : CoroutineDispatcher() {
+    // surf start - enhance kdocs
     /**
-     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
-     * The default behavior for most dispatchers is to return `true`.
-     * This method should generally be exception-safe. An exception thrown from this method
-     * may leave the coroutines that use this dispatcher in the inconsistent and hard to debug state.
+     * Returns false if already on the global tick thread, allowing inline execution.
      */
+    // surf end - enhance kdocs
     override fun isDispatchNeeded(context: CoroutineContext): Boolean {
         wakeUpBlockService.ensureWakeup()
-        return true
+        return !plugin.server.isGlobalTickThread // surf - only schedule tasks if needed
     }
 
     /**
diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MainDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MainDispatcher.kt
index 2ef825107c8fd204edc3749060360bd8d809a5c2..c85ddb441145dd964e677718fbfe3d7c23618a59 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MainDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MainDispatcher.kt
@@ -15,6 +15,13 @@ class MainDispatcher(
         Thread(r, "MCCoroutine-${plugin.name}-MainThread")
     }
     private var actionQueue = ConcurrentLinkedQueue<Runnable>()
+
+    // surf start - only schedule tasks if needed
+    /**
+     * Thread id of the dedicated main thread.
+     */
+    @Volatile
+    // surf end - only schedule tasks if needed
     var threadId = -1L
 
     init {
@@ -33,6 +40,17 @@ class MainDispatcher(
         }, 1L, tickRateMs, TimeUnit.MILLISECONDS)
     }
 
+    // surf start - only schedule tasks if needed
+    /**
+     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
+     * Returns false if we are already on the main dispatcher thread, allowing the framework to
+     * skip dispatch entirely and run the block inline.
+     */
+    override fun isDispatchNeeded(context: CoroutineContext): Boolean {
+        return Thread.currentThread().threadId() != threadId
+    }
+    // surf end - only schedule tasks if needed
+
     /**
      * Handles dispatching the coroutine on the correct thread.
      */
diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MinecraftCoroutineDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MinecraftCoroutineDispatcher.kt
index 0a7aee7e575a2753412cd76c27792bc16464bdbb..2cb7f95b19cd642d439e5d0e5953604775ce0c74 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MinecraftCoroutineDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/MinecraftCoroutineDispatcher.kt
@@ -13,15 +13,14 @@ internal open class MinecraftCoroutineDispatcher(
     private val plugin: Plugin,
     private val wakeUpBlockService: WakeUpBlockServiceImpl
 ) : CoroutineDispatcher() {
+    // surf start - enhance kdocs
     /**
-     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
-     * The default behavior for most dispatchers is to return `true`.
-     * This method should generally be exception-safe. An exception thrown from this method
-     * may leave the coroutines that use this dispatcher in the inconsistent and hard to debug state.
+     * Returns false only when already on the primary thread, allowing the framework to skip dispatch.
      */
+    // surf end - enhance kdocs
     override fun isDispatchNeeded(context: CoroutineContext): Boolean {
         wakeUpBlockService.ensureWakeup()
-        return !plugin.server.isPrimaryThread && plugin.isEnabled
+        return !plugin.server.isPrimaryThread && (plugin.isEnabled || true) // surf - only schedule tasks if needed
     }
 
     /**
diff --git a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/RegionDispatcher.kt b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/RegionDispatcher.kt
index 7e19d5da47cf660fd3542e01b4ee077bf2f4bfa8..f6914374e28f71cdc3b0ac045c6aeacb4d68d75a 100644
--- a/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/RegionDispatcher.kt
+++ b/mccoroutine-folia-core/src/main/java/com/github/shynixn/mccoroutine/folia/dispatcher/RegionDispatcher.kt
@@ -13,15 +13,14 @@ internal open class RegionDispatcher(
     private val chunkX: Int,
     private val chunkZ: Int
 ) : CoroutineDispatcher() {
+    // surf start - enhance kdocs
     /**
-     * Returns `true` if the execution of the coroutine should be performed with [dispatch] method.
-     * The default behavior for most dispatchers is to return `true`.
-     * This method should generally be exception-safe. An exception thrown from this method
-     * may leave the coroutines that use this dispatcher in the inconsistent and hard to debug state.
+     * Returns false if the current thread already owns this region, allowing inline execution.
      */
+    // surf end - enhance kdocs
     override fun isDispatchNeeded(context: CoroutineContext): Boolean {
         wakeUpBlockService.ensureWakeup()
-        return true
+        return !plugin.server.isOwnedByCurrentRegion(world, chunkX, chunkZ) // surf - only schedule tasks if needed
     }
 
     /**
